# VF-03 populated fixture qualification status

Status: **PASS — generated and re-admitted through public kit APIs**

The evidence fixture is `evidence/visual-foundation/fixture-50.roproj` (18 files,
50 rows). It is generated by `scripts/visual-foundation-generate-fixture.mjs`
using only the vendored public `createExperimentalDesignerClient()` path:
`newTracker()`, 50 `trackerCommand({type: "append_row"})` publications,
`editText`, `editNumber`, `editBoolean`, `exportCanonicalTree`, and
`openCanonicalTree`. The script then queries the re-opened table and asserts
exactly 50 rows before writing opaque exported bytes.

Observed result: `rows=50`, `files=18`, resulting revision `resident/200`,
canonical tree SHA-256
`84aeaae7aacc6fd3156d5d90391316be42845522794ba869bbcb995aa2cebc4b`.
Rows contain alternating CJK/Latin text, numeric estimates 1–9, and supported
boolean values. This is an evidence-only corpus and does not replace or modify
the original three-row M1 fixture.

The public Tracker profile does not expose a calculation-failure or unavailable
field through this authoring path; those exception presentations remain covered
by the existing projection unit tests and state board, not claimed as generated
fixture content.
