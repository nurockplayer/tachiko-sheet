# #68 Interface Profile boundary evidence

The closed boundary and the three canonical color bindings are complete under
[Steward disposition](https://github.com/nurockplayer/tachiko-sheet/issues/68#issuecomment-5774025721).
Only `text.link` (`#5542B5`), `text.reference` (`#4F54AD`) and
`grid.header.foreground` (`#5B6072`) correct prior visible implementation drift.
Canonical #71 manifests/Figma authority remain unchanged. Other appearance,
geometry, protected-state, lifecycle and document constraints retain parity.

## Tested material and local checks

Baseline: merged #74, `799c42120c3c2a2ae6a4e2ab796b33c1062d9dcc`.
Resumed checkpoint: `8717e4e88219955b14c6a030d71d8fafe7766497`.
`source-manifest.json` identifies exact tested implementation/evidence-helper
contents. The owning #68/PR records the containing Final Candidate commit and
subsequent hosted checks/fresh independent Sol receipt. Luna implementation and
WIP audits and the separate Sol consultation receive no final-review credit.

- `pnpm typecheck`: PASS.
- Focused profile/CSS units: 90 PASS; `pnpm test:unit`: 179 Vitest + 16 Node PASS.
- `pnpm build`: PASS, including production acceptance-hook exclusion (66 files).
- Focused real-kit profile probe and `pnpm acceptance:product`: PASS, including
  unchanged six M1 journeys, J3, J4 normal/imported, J5, recovery and focus.
- `WORK_DIST=dist pnpm test:visual-foundation`: PASS.
- Core pin/assets unchanged; prior qualified provenance check passed (24 assets).

The real-kit probe saves an actual J4/J5 report copy, opens a cell draft and
applies a valid manifest changing all 29 public colors. It proves stable entire
shell-subtree/control identity, focus/draft selection, zero public Work-method
and host-write delta, unchanged occurrence/revision/opaque bytes/saved bytes and
presentation attachment, and byte-identical report PNG. Four actual protected
nodes and 24 supplementary CSS recipes remain unchanged. Forced colors and
reduced motion retain precedence. PNG SHA-256:
`1cec08cd95325d175aace8c88e8e4423dad233c5c79b94bc613195ba7e76f73a`.

The completion regression exposed an asynchronous table-selection probe race.
The repaired probe waits for the real catalog price header before reading its
position; the scenario, cell, assertions and runtime boundary are unchanged.
The original failure and subsequent focused/full passing logs are retained.
Runtime observation uses public `exportOpaque` for the real format-2 J4 project;
observations stay outside the zero-Work window. No bytes are decoded or rewritten.

## Approved-delta visual evidence

Same Chromium environment, 12 widths (320, 360, 600, 720, 1023, 1024, 1280,
1440, 1512, 1920, 2560, 3840), seven states (first entry, Home, populated grid,
focus, CJK draft, Save dialog, forced colors/reduced motion). Home captures wait
for Saved copies settlement; original historical captures remain preserved.

- All 84 candidate states are pixel/PNG and computed-style identical to baseline
  when **only the three approved role values** are restored in browser test context.
  This restoration does not exist in product code.
- Unmodified canonical renders preserve sampled geometry, fonts, backgrounds and
  outlines in all 84 states. All changed pixels in the 72 changed states lie
  within the exact approved consumers' boxes, with a one-pixel paint outset for
  underline/antialias ink. All 12 forced-colors states are pixel-identical.
- Actual link and grid-header computed colors equal the canonical role values;
  selected-row headers retain the existing primary-action recipe.
- No reference cell occurs in this viewport/state fixture. A separate isolated
  browser CSS-consumer check proves canonical reference ink and role responsiveness;
  it is supplementary binding evidence, not a claimed normal reference journey.

`parity-summary.json` records hashes and counts; `historical-parity-summary.json`
preserves the pre-disposition comparison. Artifacts, immutable prior build,
captures, comparison scripts and command logs are at
`/Users/tachikoma/.codex/artifacts/tachiko-sheet-68`. Current runner logs are in
`evidence/product-acceptance`. Full comparison: `approved-consumer-comparison.json`.

## Remaining final gates

Hosted exact-head checks and fresh independent Sol review are recorded on the
owning PR after Final Candidate declaration; this document does not pre-approve
them or claim merge. No #69 production readiness, engineering RC, physical
IME/AT acceptance or production promotion is inferred. Held #40 is untouched.
