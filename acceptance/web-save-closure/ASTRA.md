# Mandatory architecture consultation disposition

Read-only GPT-6 Astra / High, agent `/root/save_closure_astra`, consulted for
live #1 storage/reopen/compatibility trigger. No production or acceptance
adequacy authorship/review credit; Sol retains engineering disposition.

Advice accepted: use all authoritative schema columns, not selected rows or
selected binding tables. Pinned Work export checks every field for Date,
including an empty third table; existing Sheet revision-checked bindingCatalog
already exposes these facts on the serialized creation path. Require fresh facts
for the invocation witness; an earlier UI catalog or failed read is not proof
of no Date. Acceptance faults concern existing typed-protocol reads and revision
coherence, not a new general malformed-reply validation contract. Preserve `reference` in the existing type vocabulary. A Date-specific
refusal must occur before producer Create and before App clears previous results
or changes truthful currentness. Do not classify unknown facts as current.

Date-only producer private export, imported metadata inspection and fresh reopen
must qualify with the unchanged private-copy reader. Existing opaque envelope
allows absent presentation; attach original source/metadata/ledger. Rollback is
client code reversion with saved copies retained, not migration/deletion.

Ordinary supported scalar edits do not change schema types; cleanup conversion
uses existing declared types, and import replaces the occurrence. No supported
same-document operation introduces Date into an already definition-bearing
document; no schema/type-edit UI, blanket edit/cleanup guard or Work API is
invented. A later concrete missing producer fact/reopen seam returns to Work374.

Sol disposition: finite fixed-pin profile gate plus existing lossless Date-only
save selection; no new semantic interpreter, producer format/API/pin/backend,
reader/envelope change or global candidate/session refactor. Work374 retains
producer Date+definition gap, Work production STOP/seam HOLD.

Conditional Pro: no remaining material architecture disagreement, new stable
public contract, breaking migration/coordinated rollout, absent rollback or
unaccepted multi-client blast is demonstrated after Astra. No actual trigger
remains for this packet. New contrary evidence requires re-entry, not a waiver.

Evidence: Work pin518aaa55e046a4e4676b4d5e05d8189c4c6343fe,
apps/designer/runtime/src/lib.rs export963, Date predicate2525, schema
collections2813, columns1094, field vocabulary3440; crates/storage/src/direct_ro/
v2.rs116. Sheet07b1d19 session.ts295 bindingCatalog,593 creation,411 imported
open inspection; App.tsx1235 creation coordination,792 open;
host/local-copies.ts423 write/281 read. Actual kit and adapter receipts in this
seed separately qualify the premises. Advice alone is not executable evidence.
