# Fixed-pin Web save closure — Steward acceptance

Status: acceptance preparation only; no product implementation; production HOLD.
Baseline Sheet 07b1d19fa1a78b73af8275beea73149f1084a0f1. Work pin
518aaa55e046a4e4676b4d5e05d8189c4c6343fe, manifest SHA256
ae82d68592b73ac5da4f72fe9242833f2e9ba15e93480fac01d5d7751b86125b.
Owning product authority: Sheet #46, single handoff #2; Work #471/#374 fixture.

One coherent client save-closure correction: Date-only Save uses an existing
producer-supported lossless copy path; with this fixed producer, Date plus a
saved grouped definition is refused before definition publication. Preserve
all prior authored work and currentness. No producer change or universal
saveability service is required unless concrete qualification shows an absent
producer-owned seam. This is a finite fixed-pin capability gate, not a second
semantic candidate interpreter. All collections' authoritative schema field
types matter, including empty Date columns unrelated to selected summary.
The existing revision-checked binding catalog already exposes these facts.

Preserve canonical selection for representable non-Date definition-free work,
existing private opaque export for definitions and the already capable Date-only
producer export; all existing private-copy readers, strict/create-only storage,
source metadata/ledger, report binding/currentness and saved acknowledgment
rules. Date+summary remains unsupported and visible; do not coerce Date to Text,
drop authored parts, mutate then export/rollback, reinterpret bytes, bump the
pin, add a format/backend, or refactor the global candidate/session architecture.

Acceptance must reach real pinned Worker/WASM, not mocks. Product actions use
normal UI; observation only may read existing acceptance invocation counters,
DOM occurrence/revision and own local records. Assert exact table types/values,
source bytes/hash/metadata bindings, fresh occurrence after full close and
reload, restored report title/labels/type/legend and current producer results.
A saved core revision alone never proves edited presentation saved.

Required scenarios:
A. Date-only import + Date scalar edit + Save + close/reload/Home reopen. All
values/types/source metadata/ledger retained; honest Saved after completed
storage; occurrence changes. Known Date-only private export plus producer
metadata inspection must first be qualified as an actual prerequisite.
B. Date plus summary in same table: refuse before createKeyedGroupedSum call,
no new definition/revision/occurrence/current-work replacement, preserve Date,
source and existing authored parts; explain the unsupported combination.
C. Date in a third collection, while summary binds two non-Date collections;
repeat B with the Date table not selected and with Date schema but no rows.
D. Non-Date definition-free canonical and definition-bearing opaque control;
source retention, report120/80/80 Unicode boundaries and legend false survive
fresh reopen; over-limit121/81/81 drafts are tested independently, leave applied report pixels unchanged and block Save.
E. Shared CSV64/16 positives save/reopen exact values;65/17 negatives refuse
inspection before replacement. Existing private-copy reader and duplicate name
regressions remain green, with original stored bytes unchanged.
F. Failed/unavailable all-collection reads and stale witness or reply revisions
under the existing pinned typed public-client protocol cannot permit publication;
no speculative retry. This packet adds no general malformed-reply validator. Qualify stale witness/bootstrap/table and unavailable unrelated-table reads
with the real adapter and actual kit, with no Create and identical core bytes.
Existing unknown/publication-recovery suites remain required unchanged candidate gates. No new protocol.

Introducing Date into an existing definition-bearing document is included only
if an ordinary current supported operation can actually do that without replacing
that document. Identify such an operation before adding a guard; otherwise
record non-reachability and do not invent schema/type-edit UI or a new Work seam.

The bounded two behaviors are one lossless-save promise, one existing application
workflow and one eventual implementation/acceptance PR. No backend/container
selection or Work repair is included. Proposed write surface: App createCopy and
createJ4 coordination and only necessary local runtime/UI plumbing; no persisted
schema/reader changes. Actual implementation ownership remains NONE until the
existing Mission Lead releases/transfers PR114 and records an eligible serial
lane/mode, consultation and independent adequacy in owning issues.

Before implementation: mandatory live #1 Astra consultation (storage/reopen/
compatibility and bounded alternative assessment). Pro only if its actual
post-Astra trigger remains. Before merge: unchanged applicable exact-head
checks and a fresh independent Oracle latest Extra High review by someone who
authored neither this acceptance nor the implementation or consulted direction.
