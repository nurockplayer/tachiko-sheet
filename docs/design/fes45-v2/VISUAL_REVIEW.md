# Author inspection log — not independent approval

## Iterations

1. Replaced the old heavy control/grid language with a cohesive violet/porcelain
   palette, modern control states, real local typography, new vector brand mark,
   independent lattice/structural lines and semantic icon wells. Rendered full Mac
   and compound state. The first pass still subordinated the document title too much.
2. Rebuilt the header hierarchy into product overline + 18px document title; increased
   cell text to 14px at the unchanged 28px pitch. Inspected Mac normal and unknown
   states. Verified denser readable text and the saved-copy/unsaved-draft separation.
3. Checked compact/narrow state construction, focus ownership, menu position, headers,
   underlines and overflow. Partial development renders were not counted as evidence.
4. Refined the selected row locator, single normal focus perimeter, semantic text
   treatment and secondary label contrast. Inspected full-size FHD, Mac and compound
   state, panned narrow retained draft, full component board and detailed tone/button
   crops. Dotted computed/reference/warning cues and invalid-focus layering remained visible.

After freezing render-producing inputs, generated all 26 PNGs twice. The second pass
matched all 26 file digests. Full-size and true 2× sources, not the 60% overview, are
the review evidence. The index names all captures; no rejected older image was relabeled.

## Author judgment

The chosen candidate has a clearer document identity, larger readable data text,
coherent small icons, calmer surfaces and more precise selection than the prior
attempts. The design intentionally stays a serious workbook instead of dressing it
as an AI dashboard. Its 12px added header height is recorded rather than concealed.

## Tests actually performed

- 26 PNG dimensions, 3 actual 2× detail crops and the separate narrow viewport/full capture.
- Live DOM checks of regions, column alignment, 400 projected scalar values, draft
  separation, action predicates, exception order, focus ownership, scroll and overflow.
- 61 author-owned browser assertions, including actual glyph faces, contrast pairs,
  emulated forced colors, responsive details and menu focus.
- Two complete same-environment PNG render passes and per-file SHA-256 comparison.
- Standalone review page unpack/load, 50-row construction, specimen switching and
  resize retaining the chosen unknown/draft specimen (in-memory browser load).

## Not claimed

No independent reviewer verdict, founder rendered-result approval, production QA,
real-device macOS/Windows/Web host evidence, screen-reader pass, live IME test,
performance qualification or canonical data/persistence acceptance. Static examples
of pending/unknown/error tuples are not proof of reachability in the product runtime.

The earlier exact cosmetic v2 contract is superseded by the direct visual-lead brief;
this is not a strictly pixel-conformant v2 candidate. Font rasterization remains
platform-specific. Portable system-font previews are labeled and are not hash evidence.

Independent review: PENDING. Implementation authority: false.
